// Espace de noms partagé avec les autres fichiers de matières
namespace Subjects {
  // Interface définissant la structure d'un professeur
  export interface Teacher {
    firstName: string;    // Prénom du professeur
    lastName: string;     // Nom de famille du professeur
  }
}
